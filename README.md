# REST API Module for Bitrix E-Shop

Модуль реализует REST API для каталога одежды, а также инструменты экспорта данных.

## 📋 Функциональность

1.  **REST API** (JSON) для получения дерева категорий, списка товаров и детальной информации.
2.  **Поддержка SKU:** Автоматический расчет цены "от", вывод торговых предложений (Цвет, Размер) с поддержкой Highload-блоков.
3.  **Консольный экспорт:** Скрипт выгрузки товаров в Excel с отправкой отчета на E-mail.
4.  **Автоматическая установка:** Инсталлятор настраивает окружение и правила маршрутизации.

## 🚀 Установка

1.  Клонируйте репозиторий в папку модулей:
    ```bash
    cd /home/bitrix/www/local/modules/
    git clone <repository_url> solution.api
    ```

2.  Перейдите в административную панель:
    `Настройки` -> `Настройки продукта` -> `Модули`.

3.  Найдите **"REST API Интернет-магазина"** (`solution.api`) и нажмите **Установить**.

> **Важно:** Инсталлятор автоматически скопирует файл `api_loader.php` в `/local/` и добавит правило в `urlrewrite.php`.

## 🛠 Технические особенности (Architecture)

### Роутинг
Модуль использует гибридную схему маршрутизации для обеспечения стабильной работы на различных конфигурациях веб-серверов (Apache/Nginx/OpenServer):

*   **`routes.php`**: Декларативное описание маршрутов согласно стандарту Bitrix D7 Routing.
*   **`api_loader.php`**: Кастомная точка входа (Entry Point), которая гарантированно инициализирует роутер и передает управление контроллерам, минуя возможные проблемы со стандартным перехватом API-запросов ядром.

### Контроллеры
Реализованы наследованием от `\Bitrix\Main\Engine\Controller`.
*   Поддержка **наследования полей** (если у SKU не заполнен артикул, он берется от родителя).
*   Умный поиск свойств (по названиям "Цвет", "Размер", "Артикул"), что делает модуль независимым от конкретных кодов инфоблока.

## 🔌 API Endpoints

Спецификация OpenAPI находится в файле `api.yaml`.

| Метод | URL | Описание |
| :--- | :--- | :--- |
| `GET` | `/api/v1/categories` | Дерево категорий каталога |
| `GET` | `/api/v1/categories/{id}/products` | Список товаров в категории (с ценой "от") |
| `GET` | `/api/v1/products/{id}` | Детальная карточка товара, галерея, SKU |

## 📊 Экспорт в Excel

Скрипт предназначен для запуска из консоли (CRON). Генерирует HTML-совместимый Excel файл.

**Расположение:** `/local/modules/solution.api/tools/export_excel.php`

**Требования для отправки почты:**
1.  В системе должен быть создан Тип почтового события: `EXPORT_PRODUCTS_FINISH`.
2.  Должен быть активный Почтовый шаблон для этого события, привязанный к текущему сайту.

**Запуск:**
```bash
php -f local/modules/solution.api/tools/export_excel.php email@example.com
